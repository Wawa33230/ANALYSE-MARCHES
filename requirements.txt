requests>=2.28
PyYAML>=6.0
# Agent IA de reclassement (optionnel) : necessaire seulement si ia.actif = true
# dans config.yaml. Sans cle API, l'outil fonctionne sans (repli sur les mots-cles).
anthropic>=0.40
